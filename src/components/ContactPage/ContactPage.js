// import { ContactListItem } from '../ContactListItem/ContactListItem';

// export const ContactListPage = ({ contacts }) => {
//   return (
//     <ul>
//       {contacts.map(contact => (
//         <ContactListItem key={contact.id} {...contact} />
//       ))}
//     </ul>
//   );
// };
